// Root layout required by Next.js App Router.
// Actual HTML shell is in app/[locale]/layout.tsx.
export default function RootLayout({ children }: { children: React.ReactNode }) {
  return children
}
